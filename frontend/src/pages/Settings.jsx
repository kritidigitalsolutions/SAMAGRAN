
export default function Settings() {
  return (
    <div className="settings-page">
      <section className="settings-page__panel">
        <p className="settings-page__eyebrow">Settings</p>
        <h2 className="settings-page__title">Platform settings</h2>
        <p className="settings-page__subtitle">Configure app preferences, integrations, and admin controls.</p>
      </section>
    </div>
  );
}

